import 'package:flutter/material.dart';

class readingNews extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Reading the News"),
      ),
      body: Center(
        child: Text("PLACEHOLDER FOT THE PAGE"),
      ),
    );
  }
}